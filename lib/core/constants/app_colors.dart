import 'package:flutter/material.dart';

class AppColors {
  // LIGHT
  static const lightBackground = [
    Color(0xFFdcecfb),
    Color(0xFFf7e8ff),
    Color(0xFFfce7f3),
  ];

  // DARK
  static const darkBackground = [Color(0xFF0F172A), Color(0xFF1E293B)];

  static const cardDark = Color(0xFF1E2329);

  // Gradients
  static const gradientBlueStart = Color(0xFF2196F3);
  static const gradientPurpleEnd = Color(0xFF7B1FA2);

  // Chips
  static const chipBlue = Color(0xFF42A5F5);
  static const chipPurple = Color(0xFFAB47BC);
  static const chipTeal = Color(0xFF26A69A);

  // Text
  static const textPrimaryLight = Colors.black87;
  static const textSecondaryLight = Colors.black54;
  static const textPrimaryDark = Colors.white;
  static const textSecondaryDark = Colors.white70;
  static const textGrey = Colors.grey;

  static const sunYellow = Color(0xFFFDD835);
  static const skyBlue = Colors.blueAccent;

  static const primary = Color(0xFF3B82F6);
  static const cardGlass = Colors.white;
  static const transparent = Colors.transparent;
  static const primaryGreen = Color(0xFF4CAF50);
}
